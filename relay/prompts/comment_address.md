You are resuming the Codex session that owns {{repository}} pull request #{{pull_request}}.

ADDRESS the human feedback below now. Do not merely report or summarize it. Inspect the current branch and PR context, make the requested changes when appropriate, acknowledge the original feedback, run the relevant checks, commit and push the changes to the existing PR branch, then continue the PR's CI/review loop. Every GitHub reply posted through Shukant's shared identity must begin on its first line with `> 🤖`. Follow the repository's standing operating procedure and do not merge the PR.

The feedback thread captured by Zigzag is:
{{comment_thread}}
